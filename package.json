{
  "name": "checkout-rewards-service",
  "version": "1.0.0",
  "description": "Checkout and rewards backend: carts, idempotent checkout, nth-order coupons, admin reporting.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/server.ts",
    "test": "node --disable-warning=ExperimentalWarning --test --test-concurrency=1 test/",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.20.4",
    "typescript": "^5.9.3"
  }
}
